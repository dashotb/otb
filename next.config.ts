import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: { domains: ["firebasestorage.googleapis.com", "images.pexels.com", "api.apiplaqueimmatriculation.com"] },

};

export default nextConfig;
